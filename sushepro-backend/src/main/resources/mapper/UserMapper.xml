<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sushepro.mapper.UserMapper">
    
    <resultMap id="userMap" type="User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    
    <select id="findByUsername" parameterType="String" resultMap="userMap">
        SELECT * FROM user WHERE username = #{username}
    </select>
    
    <select id="findById" parameterType="Integer" resultMap="userMap">
        SELECT * FROM user WHERE id = #{id}
    </select>
    
    <insert id="addUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, role, status, create_time)
        VALUES (#{username}, #{password}, #{role}, #{status}, #{createTime})
    </insert>
    
    <update id="updateUser" parameterType="User">
        UPDATE user SET
            username = #{username},
            password = #{password},
            role = #{role},
            status = #{status}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteUser" parameterType="Integer">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="findByRole" parameterType="Integer" resultMap="userMap">
        SELECT * FROM user WHERE role = #{role}
    </select>

</mapper>